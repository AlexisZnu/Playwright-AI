name: Playwright Tests

on:
    push:
        branches: [main]

jobs:
    playwright:
        runs-on: ubuntu-22.04
        steps:
            - name: Pruebas Playwirhgt
              run: echo "Ejecutando pruebas Playwright"

            - name: ver archivos disponibles
              run: ls -la

            
    repo-files:
        runs-on: ubuntu-22.04
        steps:
            - name: Descargando el codigo del repositorio
              uses: actions/checkout@v6

             
            - name: Listar archivos
              run: ls -la